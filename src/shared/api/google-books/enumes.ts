export enum Sorts {
    relevance = "relevance",
    newest = "newest"
}

export enum Subjects {
    all = "all",
    art = "art",
    biography = "biography",
    computers = "computers",
    history = "history",
    medical = "medical",
    poetry = "poetry"
}

export enum FetchTypes {
    search,
    loadMore
}